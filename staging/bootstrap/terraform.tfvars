# Staging Bootstrap Configuration

project_name = "aws-blueprint"
environment  = "staging"
aws_region   = "us-east-1"

# Add trusted account IDs if you need cross-account access
# trusted_account_ids = ["123456789012"]

# External ID for cross-account role assumption (change this to a secure value)
# external_id = "your-secure-external-id"
